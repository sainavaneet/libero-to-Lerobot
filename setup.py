from setuptools import setup, find_packages

setup(
    name="libero-to-Lerobot",
    version="0.1.0",
    description="Libero to Lerobot",
    author="Sai Navaneet",
    author_email="sainavaneet7@gmail.com",
    packages=find_packages(),  # Assumes your code is in a 'mambavla' directory
    python_requires=">=3.6",
    install_requires=[],
)